# expected: 54
# compile_args: "-I", "tests/modules"
# Import visibility, the legal shapes: the entry file imports `middle`,
# whose function reaches into `hidden` (a *direct* import of `middle`) —
# and the entry calls `middle` itself. middle_value() = 41 + 6 + 1 = 48.

$import middle

fn main(u32 argc, u8** argv) -> i32 {
    return middle_value() + 6
}
