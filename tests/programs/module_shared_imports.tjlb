# expected: 53
# compile_args: "-I", "tests/modules"
# Two files of one module share that module's imports: pair/importer.tjlb
# imports `hidden`; pair/user.tjlb uses `hidden` symbols without any
# `$import` of its own. pair_value() = 6 + 6 + 41 = 53.

$import pair

fn main(u32 argc, u8** argv) -> i32 {
    return pair_value()
}
