# expected: 103
# Test global variables
i32 counter = 100

fn increment() {
    counter = counter + 1
}

fn main(u32 argc, u8 **argv) -> i32 {
    increment()
    increment()
    increment()
    return counter  # Should be 103
}
