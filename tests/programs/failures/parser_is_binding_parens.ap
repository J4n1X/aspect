# expected: "not an expression", "unparenthesized `&&`-conjunct"
# C muscle memory: parens flip the binding form into expression position.

sum S {
    A(i32 x)
    B
}

fn main(u32 argc, u8 **argv) -> i32 {
    S s = S.B
    if (s is A(v)) {
        return v
    }
    return 0
}
