# expected: "function 'hidden_value' is defined in module 'hidden'", "the root module does not import"
# compile_args: "-I", "tests/modules"
# Imports do not trickle down: the entry imports `middle`, `middle`
# imports `hidden` — calling a `hidden` function from the entry is an
# error naming both modules.

$import middle

fn main(u32 argc, u8** argv) -> i32 {
    return hidden_value()
}
