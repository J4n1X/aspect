# expected: "empty pattern parens", "ignores the payload"

sum S {
    A(i32 x)
    B
}

fn main(u32 argc, u8 **argv) -> i32 {
    S s = S.B
    switch s {
        case A() { return 1 }
        case B { return 0 }
    }
    return 0
}
