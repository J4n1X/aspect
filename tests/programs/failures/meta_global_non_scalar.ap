# expected: "meta global", "integer or bool"
#-
  A `meta` global must be an integer or bool in v1 — the compile-time store is a
  scalar. A float (or pointer/struct) type is rejected at parse time.
-#
meta f32 ratio = 0.0

fn main(u32 argc, u8 **argv) -> i32 {
    return 0
}
