# expected: "signature", "Program"
# compile_args: "-I", "lib"
#-
  A rule fn used as a checker must be `(Program, Type) -> Judgments`.
-#

rule fn bad(i32 x) -> i32 { return x }
type Config { public i32 x }
rule Config bad

fn main(u32 argc, u8 **argv) -> i32 { return 0 }
