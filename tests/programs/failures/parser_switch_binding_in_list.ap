# expected: "must be the arm's only pattern"
# A binding pattern cannot share an arm with alternatives — there is no
# binding unification.

sum S {
    A(i32 x)
    B
}

fn main(u32 argc, u8 **argv) -> i32 {
    S s = S.B
    switch s {
        case S.A(v), S.B { return 0 }
    }
    return 0
}
