# expected: "undefined identifier", "MYSTERY"
# An undefined identifier in a `$if` expression is a hard error — never
# silently 0 (C's silent-zero rule is a famous bug factory).

$if MYSTERY > 2
$endif

fn main(u32 argc, u8 **argv) -> i32 {
    return 0
}
