# expected: "must be a", "transform fn"
#-
  A `transform` binding whose handler is an ordinary function — not a
  `transform fn` with signature `(Expr) -> Expr` — is rejected before
  elaboration. The handler must be a real transform hook.
-#
type Str {
    public u8* ptr
}

fn identity(u64 x) -> u64 { return x }
transform Str -> u8* identity

fn main(u32 argc, u8 **argv) -> i32 {
    return 0
}
