# expected: "extern functions cannot be public"
# `extern` names a symbol defined elsewhere, so there is nothing here to export.

public extern fn puts(u8 *s) -> i32

fn main(u32 argc, u8 **argv) -> i32 {
    return 0
}
