# expected: "empty payload parens", "payload-less variant is a bare name"
# A payload-less variant is written bare (`Dot`), never `Dot()`.

sum Shape {
    Dot()
}

fn main(u32 argc, u8 **argv) -> i32 {
    return 0
}
