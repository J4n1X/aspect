# expected: "duplicate case variant 'A'"

sum S {
    A(i32 x)
    B
}

fn main(u32 argc, u8 **argv) -> i32 {
    S s = S.B
    switch s {
        case S.A(v) { return v }
        case S.A(w) { return w }
        case S.B { return 0 }
    }
}
