# expected: "cannot be combined on one function"
# A function is exactly one kind: `naked` and `asm` name two different bodies
# (raw ABI vs pinned-register operands), so combining them is one error.

naked asm fn f() {
    "ret"
}

fn main(u32 argc, u8 **argv) -> i32 {
    return 0
}
