# expected: "type mismatch", "u8", "i32"
#- literal_overflow.tjlb
   This program must be REJECTED by the typechecker.
   300 does not fit in u8 (range 0..=255).
-#

fn main() -> i32 {
    u8 bad = 300  # ERROR: 300 > 255, out of range for u8
    return 0
}
