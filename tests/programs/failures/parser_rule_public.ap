# expected: "rule", "public"
#-
  A rule cannot carry `public` (or `export`) — rules are always whole-program.
  Rejected at parse time.
-#

type Config {
    public i32 value
}

public rule Config singleton

fn main(u32 argc, u8 **argv) -> i32 {
    return 0
}
