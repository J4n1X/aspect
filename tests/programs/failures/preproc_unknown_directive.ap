# expected: "unknown preprocessor directive", "defien", "did you mean `$define`"
# An unrecognised directive name errors with a did-you-mean suggestion
# computed against the directive table.
$defien MAX_SIZE 1024

fn main(u32 argc, u8 **argv) -> i32 {
    return 0
}
