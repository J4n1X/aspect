# expected: 13
# Test recursive function
fn fib(i32 n) -> i32 {
    if n <= 1 {
        return n
    }
    return fib(n - 1) + fib(n - 2)
}

fn main(u32 argc, u8 **argv) -> i32 {
    return fib(7)  # Should be 13
}
