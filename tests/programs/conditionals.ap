# expected: 50
# Test if/else
fn max(i32 a, i32 b) -> i32 {
    if a > b {
        return a
    } else {
        return b
    }
}

fn main(u32 argc, u8 **argv) -> i32 {
    i32 x = max(15, 20)
    i32 y = max(30, 25)
    return x + y  # Should be 20 + 30 = 50
}
